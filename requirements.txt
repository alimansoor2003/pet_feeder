flask
ultralytics
torch
torchvision
pillow
numpy
# Headless build: same API minus the GUI window functions (imshow, waitKey),
# none of which are used here — webcam_watcher.py only calls VideoCapture,
# imencode and cvtColor. The regular opencv-python wheel links against X11
# desktop libraries (libxcb.so.1) that server containers don't ship, so
# ultralytics' `import cv2` crashed the app at startup on Railway.
opencv-python-headless
timm
waitress
psycopg[binary,pool]
python-dotenv
